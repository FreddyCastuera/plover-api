const express = require('express');
const Payments = require('../usecases/payments');
const router = express.Router()


/*router.post('/', async (request, response)=> {
    try{
        const newPayment = request.body;
        const paymentCreated = await 
    }
})*/
